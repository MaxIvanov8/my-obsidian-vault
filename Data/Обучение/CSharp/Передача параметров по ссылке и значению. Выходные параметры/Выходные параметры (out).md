Чтобы сделать параметр выходным, перед ним ставится модификатор **out**. Здесь результат возвращается не через оператор `return`, а через выходной параметр result.
```
void Sum(int x, int y, out int result)
{
    result = x + y;
}
```
