**Нормальное распределение** — это тип теоретического распределения, в котором значения в основном сосредоточены вокруг среднего. Это распределение имеет форму колокола и описывается двумя параметрами: средним значением μ и дисперсией σ2.

Функция плотности вероятности для нормального распределения всегда симметрична, имеет знаменитую форму колокола и определяется двумя параметрами: μ (читается как «мю») и σ (читается как «сигма»).
- Параметр μ отвечает за среднее значение или, иными словами, за центр распределения, над которым находится пик колокола.
- Параметр σ есть стандартное отклонение (оно же есть корень из дисперсии) и отвечает за разброс значений в распределении, что отражается в ширине колокола.

$$
X∼N(μ, σ^2)
$$
![[Pasted image 20241226165121.png]]
Можно сформировать такие правила:
- Чем больше μ — тем правее будет центр колокола, чем меньше — тем левее.
- Чем больше σ — тем шире будет форма колокола, чем меньше — тем уже.

Правило «трёх сигм»:
- В нормальном распределении примерно 68.3 всех значений находятся в пределах одного стандартного отклонения от среднего (μ±σ).
- Примерно 95.4% значений находятся в пределах двух стандартных отклонений (μ±2⋅σ).
- И примерно 99,7% значений находятся в пределах трёх стандартных отклонений (μ±3⋅σ).
![[Pasted image 20241226165402.png]]
Это правило используется во многих задачах. Например, если нам дали выборку и сказали, что её данные распределяются по нормальному закону с такими-то параметрами, то мы можем проверить: а не отклоняется ли некоторое определённое значение дальше, чем, например, 2 сигмы. Если вдруг окажется, что это так, то мы можем сделать вывод, что это выброс.

### Стандартное нормальное распределение

**Стандартное нормальное распределение** — частный случай нормального распределения, когда μ=0, σ=1.

$$
X∼N(μ, σ^2)

Z∼N(0, 1).
$$
![[Pasted image 20241226165729.png]]
95% наиболее вероятных значений такой случайной величины будут располагаться в интервале между −1.96−1.96 и 1.96.1.96.
